# 待办事项

- 加载动画

## 浏览器手动验收清单（2026-09-26 修复计划合并前）

> 验收对象：dev 预览（`https://dev-navbase.1099116749.workers.dev`）或本地 `pnpm dev:full`。
> 每项按步骤操作，✅ 勾选通过项；任一项失败记在该项下方并回报。

### 0. 构建门禁确认（30 秒）

- [ ] 控制台三处构建命令均为 `pnpm test && pnpm run build`：Workers & Pages → navbase → Settings → Build 的 **生产**、**Previews Base**，以及顶部下拉切到 **dev** 后构建配置里的那份
- [ ] （可选验证）本地跑一次 `pnpm test && pnpm run build` 应全绿；故意改挂一个测试再跑应失败——证明门禁命令本身有效

### 1. 加载失败横幅与重试（任务 15）

- [ ] 打开 DevTools → Network 勾选 **Offline**，刷新首页
- [ ] 预期：出现「数据加载失败…」横幅 + **重试** 按钮；**不出现**「暂无书签」空态误导；页脚计数不假装是 0
- [ ] 点「重试」（仍断网）：横幅保持、不闪空态
- [ ] 恢复网络再点「重试」：横幅消失、数据正常加载

### 2. 大批量导入分块（任务 16）

- [ ] 准备一份 > 500 条链接的浏览器书签 HTML 导出（或临时脚本生成 501 条）
- [ ] 设置面板 → 导入 → 选该文件
- [ ] 预期：不报「单次最多导入 500 条」；全部条目分批入库；成功文案的「已导入 X 个 / 跳过 N 条重复 / 新建 N 个分类」数字正确
- [ ] （可选）中途断网制造某批失败：提示「已导入前 N 批，第 M 批失败」，已入库的批次保留

### 3. 导航与登录回跳（任务 19）

- [ ] 在首页点顶栏 **logo**：回首页且 Network 面板**无 document 整页请求**（纯 SPA 导航）
- [ ] 地址栏访问 `/foo`：自动跳回首页（不再白屏）
- [ ] 修改密码（或删掉密码）后触发任意 API：被踢到登录页；重新登录后**回到刚才所在页面**（redirect 回跳）
- [ ] 弱网/断网下提交表单：约 15 秒后报「请求超时，请检查网络后重试」，按钮不再永久「保存中…」

### 4. 弹窗焦点陷阱与防误关（任务 18 + 20）

- [ ] 打开「设置」弹窗：焦点落在**关闭按钮**上
- [ ] 连续按 **Tab**：焦点在弹窗内循环，不会跑到遮罩背后；**Shift+Tab** 反向同理
- [ ] 鼠标点一下弹窗内的空白/标题文字，再按 Tab：焦点**仍被弹窗接住**（不逃到背景）
- [ ] 删除书签确认框弹出后立刻点「删除」，loading 期间：点遮罩 / 按 ESC / 点关闭——弹窗**都不会关**
- [ ] 分类/书签表单提交 loading 期间：ESC 与遮罩点击同样不能关窗
- [ ] 弹窗正常关闭后（非 loading）：ESC、遮罩、关闭按钮均正常生效；关闭后焦点回到打开前的元素

### 5. 键盘与快捷键（任务 20）

- [ ] `Alt+N` 弹「新建书签」，`Alt+Shift+N` 弹「新建分类」，`Alt+K` 聚焦搜索
- [ ] 若有欧洲键盘布局（AltGr）：`AltGr+N` 应正常输入字符（如 ń），**不弹**新建书签
- [ ] 搜索框内 Tab 切换引擎的既有行为不受影响

### 6. Toast 计时（任务 20）

- [ ] 2 秒内连续点两次「复制链接」：第二条提示应完整显示约 2.4 秒（不提前消失）

### 7. favicon 与固定功能冒烟（任务 1-2 + 旧功能）

- [ ] 书签库与首屏的图标正常显示（新缓存键 v2 生效后首次加载略慢属正常）
- [ ] 右键书签「固定到首屏 / 取消固定」双向正常；固定第 11 个时提示「最多固定 10 个书签」
- [ ] 首屏常用站点宽屏 5 列、最多两行

### 8. 合并后收尾（验收全过之后）

- [ ] 合并 dev → main 上线（迁移已于合并前执行）
- [ ] 让我或自行把 CLAUDE.md 门禁措辞从「应设为」改回「已配置」（构建命令配置完成后）
