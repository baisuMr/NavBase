# 待办事项

> 来源：2026-09-23 全项目深度评审（当时修复 6 项：favicon 重定向域名白名单、删除分类事务化、拖动排序失败提示、favicon 文档、sortablejs 动态加载、HTML 截断读取）；2026-09-24 全面梳理又修复 7 项（favicon 拒收 SVG、分类图标回退接线、CORS 样板删除、dist 冗余字体裁剪、Esc 关确认弹窗、样式 token 化、登录响应文档补齐）。以下为剩余待办，按需择机处理。

## 性能

- [ ] **remixicon CSS 子集化**（可选，需取舍）：remixicon 全量 CSS 打包 164KB（gzip 26KB），实际仅用约 50 个图标，子集化可省约 15KB gzip。但现有约定是「新增图标直接写 `ri-xxx-line` class，无需重跑脚本」——子集化会破坏该便利性，需在 `scripts/generate-fonts.mjs` 旁增加图标清单维护脚本。若嫌维护成本高，保持现状合理（26KB 对现代网络可接受）。（非 woff2 字体格式已于 2026-09-24 裁掉，部署产物不再携带约 4.5MB 冗余字体。）

## 测试

- [ ] **拖动排序交互自动化测试**：`BookmarkExplorer.vue` 的 Sortable 集成逻辑（onMove 约束、DOM 还原 hack、索引映射）无自动化覆盖。happy-dom/jsdom 无法模拟 Sortable 依赖的布局计算；Playwright 实测会改动本地 D1 真实 sort_order 数据。当前靠人工回归 + `moveInArray`/后端 sort 端点的单元测试兜底。若未来重构该交互，先搭 Playwright 回归用例（可用独立的临时 D1 库）。

## 观察项（无需行动，仅记录）

- **删除分类时书签变未分类**：2026-09-23 已与用户确认**保持现状**（书签 `category_id` 置空，零改动）。备选方案已评估：删除确认弹窗加分类下拉、书签批量迁移到所选分类（后端 `DELETE /api/categories/:id` 加可选 `target_category_id`，约 30 行改动）。分类栏已新增「未分类」tab（`aa24966`）可集中查看与整理未分类书签，方案紧迫性进一步降低；若未来仍有批量迁移需求可再启用。
- **`useFavicon` 的 failedAt Map 无垃圾回收**：书签删除后对应失败记录永不清理（TTL 过期依赖"下次渲染该书签时惰性删除"）。残留量微小（id → 时间戳），自用规模无碍。若在意可在删除书签处联动清理。
- **「常用站点」语义**：第一屏常用站点卡片取 `sort_order` 前 5，即**最早收藏的 5 个**，并非按使用频率。若想要"最常用"，需新增访问计数（点击埋点 + D1 计数表），属功能设计变更，看产品意愿。
